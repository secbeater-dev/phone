# 2026-09-16 匯入後首頁調整驗證

## 變更

- 基準提交：`c96a7b453459893fd23c218bf6728091934154fd`。
- 發布版本：`20260916-import-hours-v1`。
- 一般檔案與 workspace JSON 成功匯入後開啟「時間分布圖」，同步 Worker 資料集與舊版相容流程。原本已在時間圖時重新繪製新資料。
- 部分成功採用成功資料並顯示時間圖；全部失敗或取消保留目前頁面。多門號位置維持獨立。
- 同步資產版本並重新計算 CSP／SRI。未變更解析器、資料格式或匯出介面。

## 本機合成測試

- 新增匯入首頁測試先在舊程式執行，2 項均因仍停在通聯列表而失敗；調整後 2 項通過。驗證實際頁面、導覽狀態與 24 小時統計值，包含重新匯入後舊時段歸零。
- `npm test`：65 項通過、2 項私密 opt-in 略過。
- `PHONE_BROWSER_CHANNEL=chromium-headless-shell npm run test:browser`：15 項通過、5 項 opt-in 略過。涵蓋首頁、圖表、格式切換、部分成功、全部失敗、JSON、取消、分頁、篩選、匯出與同源網路請求。
- `PHONE_BROWSER_CHANNEL=msedge PHONE_PERF=1 node --test tests/browser/performance.test.js`：通過；首次時間圖完成後再檢查列表，10 萬筆約 25 秒、20 萬筆約 52 秒；最大 UI 計時延遲 103／178 毫秒，匯出取消約 125 毫秒。時間圖筆數正確，列表每頁 500 筆。
- 測試僅使用合成資料。未啟用私密資料與歷史像素比對測試；效能數字只代表本機 Edge 環境。

## 發布驗收

GitHub Pages 工作流程會執行 Node 與 Linux Chromium 回歸，通過後部署；部署完成另核對正式站發布版本、21 個執行資產 SHA-256、入口 SRI／CSP，並在正式站重新執行合成瀏覽器測試。實際部署連結與正式站驗收結果記錄於本機更新日誌。
